# GhostStream - Specter-Native Transcoding Service
# Runtime: Flask + gevent (Specter-owned)

# HTTP Server (Specter-native: Flask + gevent)
flask>=3.0.0
gevent>=24.2.1
gevent-websocket>=0.10.1

# Configuration
pyyaml>=6.0.1
pydantic>=2.6.0
pydantic-settings>=2.2.0

# mDNS / Zeroconf
zeroconf>=0.131.0

# HTTP Client (source fetching, registration, callbacks)
httpx>=0.27.0

# Logging
python-json-logger>=2.0.7

# TUI Dashboard
textual>=0.50.0
rich>=13.7.0

# Utilities
psutil>=5.9.7

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-aiohttp>=1.0.0
